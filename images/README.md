# Site images

This folder **must** be uploaded to GitHub with your site files.

- `profile.png` — your photo (used on every page)

If photos are missing on GitHub Pages, this folder was probably not included in your push. Run from the website folder:

```bash
git add images/profile.png
git commit -m "Add profile photo"
git push
```
